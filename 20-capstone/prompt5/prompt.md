Create a function that does cosine vector search on the embedding column for a given query.

Embed the query and the do cosine similarity returning the top 3 closest post _id - feel free to add what you feel is best.

Create a rest api that returns these 3 closets post ids for a given query supplied

Add these to the supplied plugin file